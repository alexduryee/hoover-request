<%# shared modal(s) %>
<% if @result.respond_to?(:cite) && @result.cite_item && @result.cite_item_description %>
	<%= render partial: 'cite/modal_tabbed', locals: {:modal_id => 'cite_modal', :title => t('actions.cite'), :item => @result.cite_item, :description => @result.cite_item_description } %>
	<script type ="text/javascript" >setupCite("cite_modal", "<%= t('actions.clipboard') %>")</script>
<% end %>
<%# AD - no more request actions, since we don't generate a form - instead we just go to Aeon %>